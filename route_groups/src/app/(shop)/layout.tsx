const shopLayout = ({children}: {children : React.ReactNode}) => {
    return (
        <html>
            <body>
                <h2>ShopLayout</h2>
                {children}
            </body>
        </html>
    )
}

export default shopLayout